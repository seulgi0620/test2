<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
	<select id="proc_login" parameterType="map" statementType="CALLABLE">
		{call proc_login(#{mem_id, mode=IN, jdbcType=VARCHAR, javaType=java.lang.String}
						,#{mem_pw, mode=IN, jdbcType=VARCHAR, javaType=java.lang.String}
						,#{r_name, mode=OUT, jdbcType=VARCHAR, javaType=java.lang.String})}
	</select>
	<select id="deptTest" parameterType="com.vo.DeptVO" resultType="deptVO">
		select 1 from dual
	</select>

</mapper>
